# 🏋️ Gym Bot - Telegram бот для организации походов в спортзал

Telegram бот для создания опросов о посещении спортзала. Позволяет участникам группового чата голосовать за дни недели, когда они планируют пойти в зал.

## 📋 Возможности

- ✅ Создание не анонимных опросов с выбором дней недели
- 📊 Один опрос на неделю (защита от дубликатов)
- 💾 База данных SQLite для отслеживания опросов
- 🔍 Быстрый поиск опроса текущей недели
- 📅 Автоматическое определение недели года

## 🚀 Установка

### Требования

- Go 1.21 или выше
- Telegram аккаунт
- Токен бота от [@BotFather](https://t.me/BotFather)

### Шаги установки

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd gym-bot
```

2. **Установите зависимости:**
```bash
go mod download
```

3. **Создайте файл `.env`:**
```bash
cp .env.example .env
```

4. **Получите токен бота и настройте его для групп:**
   - Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
   - Отправьте команду `/newbot`
   - Следуйте инструкциям для создания бота
   - Скопируйте полученный токен
   
   **Важно! Настройте бота для работы в группах:**
   - Отправьте [@BotFather](https://t.me/BotFather) команду `/mybots`
   - Выберите вашего бота
   - Нажмите **Bot Settings** → **Group Privacy**
   - Выберите **Disable** (отключить приватность)
   - Это позволит боту видеть все сообщения в группе и реагировать на команды

5. **Добавьте токен в `.env`:**
```env
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

6. **Запустите бота:**
```bash
go run .
```

Или соберите и запустите исполняемый файл:
```bash
go build -o gym-bot
./gym-bot
```

## 📱 Использование

### Добавление бота в группу

1. **Добавьте бота в групповой чат:**
   - Откройте групповой чат в Telegram
   - Нажмите на название группы → Добавить участников
   - Найдите вашего бота по username и добавьте его

2. **Убедитесь в правильных настройках:**
   - Бот должен иметь права на отправку сообщений (обычно установлены по умолчанию)
   - Проверьте, что Group Privacy отключена в [@BotFather](https://t.me/BotFather) (см. инструкцию выше)
   
3. **Протестируйте бота:**
   - Отправьте команду `/start` в группе
   - Бот должен ответить приветственным сообщением
   - Если бот не отвечает, проверьте настройки приватности в BotFather

### Команды

#### `/start`
Показывает приветственное сообщение и список доступных команд.

```
/start
```

#### `/newpoll`
Создаёт новый опрос на текущую неделю. Опрос содержит:
- Все дни недели (Понедельник - Воскресенье)
- Вариант "Не могу" для тех, кто не сможет пойти

**Ограничения:**
- Можно создать только один опрос на неделю
- Опрос не анонимный (видно, кто голосовал)
- Поддерживается множественный выбор

```
/newpoll
```

#### `/findpoll`
Отправляет ответ на сообщение с опросом текущей недели. Это помогает быстро найти опрос в истории чата.

```
/findpoll
```

#### `/help`
Показывает справку по доступным командам.

```
/help
```

## 🗄️ База данных

Бот использует SQLite базу данных (`polls.db`), которая автоматически создаётся при первом запуске.

### Структура таблицы `polls`

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Уникальный идентификатор |
| chat_id | INTEGER | ID чата в Telegram |
| message_id | INTEGER | ID сообщения с опросом |
| poll_id | TEXT | ID опроса Telegram |
| week_number | INTEGER | Номер недели в году |
| year | INTEGER | Год |
| created_at | DATETIME | Время создания |

### Уникальные ограничения

- Только один опрос на комбинацию (chat_id, week_number, year)
- Уникальный poll_id для каждого опроса

## 📦 Зависимости

- [`github.com/go-telegram-bot-api/telegram-bot-api/v5`](https://github.com/go-telegram-bot-api/telegram-bot-api) - Telegram Bot API для Go
- [`github.com/mattn/go-sqlite3`](https://github.com/mattn/go-sqlite3) - SQLite драйвер для Go
- [`github.com/joho/godotenv`](https://github.com/joho/godotenv) - Загрузка переменных окружения из .env файла

## 🛠️ Разработка

### Структура проекта

```
gym-bot/
├── main.go          # Основная логика бота
├── database.go      # Работа с базой данных
├── go.mod           # Go модули
├── .env.example     # Пример конфигурации
├── .gitignore       # Игнорируемые файлы
├── README.md        # Документация
└── polls.db         # База данных (создаётся автоматически)
```

### Запуск в режиме разработки

Для автоматической перезагрузки при изменении файлов используйте:

```bash
# Установите air (если еще не установлен)
go install github.com/cosmtrek/air@latest

# Запустите с hot reload
air
```

Или просто:
```bash
go run .
```

## 🔒 Безопасность

- **Никогда не коммитьте файл `.env`** с реальным токеном бота
- Файл `.env` уже добавлен в `.gitignore`
- Используйте `.env.example` как шаблон для других разработчиков

## 🐛 Troubleshooting

### Бот не отвечает на команды в группе
- **Проверьте настройки приватности:** Откройте [@BotFather](https://t.me/BotFather), выберите `/mybots` → ваш бот → **Bot Settings** → **Group Privacy** → выберите **Disable**
- Убедитесь, что токен в `.env` файле правильный
- Проверьте, что бот добавлен в группу как участник
- Убедитесь, что бот имеет права на отправку сообщений
- Попробуйте удалить бота из группы и добавить заново после отключения Group Privacy

### Бот работает в личных сообщениях, но не в группе
Это означает, что у бота включена Group Privacy. Отключите её в [@BotFather](https://t.me/BotFather):
- `/mybots` → выберите бота → **Bot Settings** → **Group Privacy** → **Disable**

### Ошибка "message to reply not found"
Это происходит, когда опрос был удалён из чата. Используйте `/newpoll` со следующей недели.

### Ошибка подключения к базе данных
Убедитесь, что у приложения есть права на запись в текущую директорию.

## 📝 Примеры использования

### Типичный workflow

1. **В начале недели:** Участник создаёт опрос
   ```
   /newpoll
   ```

2. **В течение недели:** Участники голосуют за дни, когда планируют пойти в зал

3. **Позже:** Если нужно найти опрос в истории
   ```
   /findpoll
   ```

4. **Следующая неделя:** Процесс повторяется

## 📄 Лицензия

MIT License

## 🤝 Вклад

Пул-реквесты приветствуются! Для больших изменений, пожалуйста, сначала откройте issue для обсуждения.

## ✨ Автор

Создано с помощью Yandex Code Assistant